*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/static
/public/storage
# Statamic CP assets are build artifacts: the Dockerfile runs
# `php artisan statamic:install` (after COPY) which republishes them from the
# version-matched Composer package, so they're regenerated in the image and
# don't need committing.
/public/vendor/statamic
/storage/*.key
/storage/pail
/users/*
!/users/.gitkeep
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db